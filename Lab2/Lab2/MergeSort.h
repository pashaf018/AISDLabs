#pragma once
#include "Includes.h"
class MergeSort
{
public:
	static void mergeSort(std::vector<int>&,int,int);

	static void mergeSorting(std::vector<int>*);

	static void merge(std::vector<int>&,int,int,int);
};

